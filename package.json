{
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^4.4.1",
    "pulp": "^11.0.2",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "systemd-journald": "^1.5.1"
  }
}
